# Amdox ERP — Frontend Environment Variables
# Copy to .env in production if you want to hardcode the API gateway endpoint.

# URL of the deployed backend API server. If left empty, API requests will use relative paths.
VITE_API_URL=https://api.amdox.io
